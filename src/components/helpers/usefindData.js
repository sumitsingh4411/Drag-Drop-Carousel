import axios from 'axios';
import { useEffect, useState } from 'react';

const usefindData = (mydata) => {
    // eslint-disable-next-line react-hooks/rules-of-hooks
    const [data,setData]=useState([]);
    // eslint-disable-next-line react-hooks/rules-of-hooks
   
    return {data}
}


export default usefindData;